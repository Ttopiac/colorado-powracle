# ── Core dependencies ─────────────────────────────────────────────────────────
langchain==1.2.10
langchain-classic==1.0.1
langchain-openai==1.0.3
langchain-community==0.4.1
langchain-core==1.2.16
python-dotenv
duckdb
pandas
pyarrow
requests
streamlit
plotly
google-search-results

# ── User accounts (optional) ──────────────────────────────────────────────────
# Only needed if using the PostgreSQL user accounts feature.
# Requires a running Postgres instance — see ONBOARDING.md section 1a for setup.
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
bcrypt>=4.0.0
